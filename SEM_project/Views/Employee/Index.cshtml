@model IEnumerable<SEM_project.Models.Entities.Employee>

@{
    ViewData["Title"] = "Index";
}

@if (@TempData["AlertMessage"] != null)
{
    <div class="alert-success jumbotron">
        <strong>Success !</strong> @TempData["AlertMessage"]
    </div>
}

@if (@TempData["ErrorMessage"] != null)
{
    <div class="alert-danger jumbotron">
        <strong>Error !</strong> @TempData["ErrorMessage"]
    </div>
}

<style>

    .thNew {
        background-color: #326091;
        color: white;
    }

    .thmov {
        background-color: #605F5F;
        color: white;
    }

</style>

<h1 style="color : #326091"><b>Funcionarios</b></h1>

<p>
    <a asp-action="Create">Crear Nuevo Funcionario</a>
</p>

<!-- Main content -->
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="table" class="table table-bordered table-striped">
                        <thead>
                            <tr class="thNew">
                                <!-- <th>@Html.DisplayNameFor(model => model.EmployeeId)</th>-->
                                <th>@Html.DisplayNameFor(model => model.EmployeeName)</th>
                                <th>@Html.DisplayNameFor(model => model.IDNumber)</th>
                                <!-- <th>@Html.DisplayNameFor(model => model.AssignedEquipmentPlate)</th>-->
                                <th>@Html.DisplayNameFor(model => model.EnumPosition)</th>
                                <th>@Html.DisplayNameFor(model => model.EnumAffiliation)</th>
                                <th>@Html.DisplayNameFor(model => model.EnumSubdepartment)</th>
                                <th>@Html.DisplayNameFor(model => model.EnumWorkGroup)</th>
                                <th>@Html.DisplayNameFor(model => model.IsActive)</th>
                                <!-- Add more headers for other properties as needed -->
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <!-- <td>@Html.DisplayFor(modelItem => item.EmployeeId)</td>-->
                                    <td>@Html.DisplayFor(modelItem => item.EmployeeName)</td>
                                    <td>@Html.DisplayFor(modelItem => item.IDNumber)</td>
                                    <!-- <td>@Html.DisplayFor(modelItem => item.AssignedEquipmentPlate)</td>-->
                                    <td>@Html.DisplayFor(modelItem => item.EnumPosition)</td>
                                    <td>@Html.DisplayFor(modelItem => item.EnumAffiliation)</td>
                                    <td>@Html.DisplayFor(modelItem => item.EnumSubdepartment)</td>
                                    <td>@Html.DisplayFor(modelItem => item.EnumWorkGroup)</td>
                                     <td>@Html.DisplayFor(modelItem => item.IsActive)</td>
                                    <!-- Add more columns for other properties as needed -->
                                    <td>
                                        <a class="fa fa-pencil-alt" asp-action="Edit" asp-route-id="@item.EmployeeId" title="Editar"></a> |
                                        <a class="fa fa-eye" asp-action="Details" asp-route-id="@item.EmployeeId" title="Detalles"></a> |
                                        <a class="fa fa-unlock-alt" asp-action="Delete" asp-route-id="@item.EmployeeId" title="Inactivar"></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->
</section>
<!-- /.content -->
